# Application Environment Configuration
# All sensitive credentials should be configured securely at runtime if needed.

# GEMINI_API_KEY: Required only for server-side Gemini AI features.
GEMINI_API_KEY=

# APP_URL: Host URL of the deployed application.
APP_URL=

